# Local .env example for development
# Copy to .env and replace secrets before running in any shared environment

# PostgreSQL connection (local docker-compose service uses these defaults)
DATABASE_URL=postgres://iitd_drop:local-only-change-me@localhost:5432/iitd_drop
DATABASE_SSL=false
DB_POOL_SIZE=10

# Required application secrets
SESSION_SECRET=change-me
OTP_SECRET=change-me

# Admin user (generate ADMIN_PASSWORD_HASH using scripts/hash-password.mjs)
ADMIN_EMAIL=operator@example.invalid
ADMIN_PASSWORD_HASH=replace-with-argon2-hash

# Optional runtime flags
# Razorpay modes: demo (no charge), test (Razorpay test keys), live (live keys)
RAZORPAY_MODE=demo
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
# SMS providers: demo (fixed/local response), msg91 (own DLT template), or msg91-widget (MSG91's template, no DLT)
SMS_PROVIDER=demo
MSG91_AUTHKEY=
MSG91_TEMPLATE_ID=
# OTP widget (SMS_PROVIDER=msg91-widget): MSG91 owns the DLT template, so no DLT registration
# is needed. Sending and verifying both run server-side, so nothing reaches the browser.
MSG91_WIDGET_ID=
# Demo-only fixed OTP; local preview only; never set this in production
DEMO_OTP=202626
# Browser-safe key for Places autocomplete; in production use the App Service Key Vault reference

# For production consider using Azure Key Vault or App Service managed settings rather than a file
